---
import BaseLayout from '../layouts/BaseLayout.astro';
import { projects } from '../data/site';

const currentProjects = projects.filter(p => p.current);
const pastProjects = projects.filter(p => !p.current);
---

<BaseLayout title="Projects — Suryansh Sijwali">
  <section class="profile">
    <h1>Projects and Research</h1>
  </section>

  <section>
    <h2>Currently Working On</h2>
    <div class="project-grid">
      {currentProjects.map((project) => (
        <div class="card project-card">
          <h3>
            <a href={project.url} target={project.url.startsWith('/') ? '_self' : '_blank'} rel="noopener">
              {project.title}
            </a>
          </h3>
          <p>{project.description}</p>
          <div style="display: flex; gap: 0.5rem; flex-wrap: wrap; margin-bottom: 0.75rem;">
            {project.tags.map((tag) => (
              <span class="tag">{tag}</span>
            ))}
          </div>
          <div class="project-links">
            {project.github && (
              <a href={project.github} target="_blank" rel="noopener">Code</a>
            )}
            {!project.url.startsWith('/') && (
              <a href={project.url} target="_blank" rel="noopener">Live</a>
            )}
          </div>
        </div>
      ))}
    </div>
  </section>

  <section>
    <h2>Past Work (Shipped)</h2>
    <div class="project-grid">
      {pastProjects.map((project) => (
        <div class="card project-card">
          <h3>
            <a href={project.url} target={project.url.startsWith('/') ? '_self' : '_blank'} rel="noopener">
              {project.title}
            </a>
          </h3>
          <p>{project.description}</p>
          <div style="display: flex; gap: 0.5rem; flex-wrap: wrap; margin-bottom: 0.75rem;">
            {project.tags.map((tag) => (
              <span class="tag">{tag}</span>
            ))}
          </div>
          <div class="project-links">
            {project.github && (
              <a href={project.github} target="_blank" rel="noopener">Code</a>
            )}
            {!project.url.startsWith('/') && (
              <a href={project.url} target="_blank" rel="noopener">Live</a>
            )}
          </div>
        </div>
      ))}
    </div>
  </section>
</BaseLayout>
